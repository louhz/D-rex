# Python version is managed outside pip: use Python 3.10
# Match the CUDA 11.8 PyTorch wheel index.
# PyTorch's official pip combo for torch 2.0.0 + CUDA 11.8 uses torchvision 0.15.1,
# even though the conda env used torchvision 0.15.0.

--extra-index-url https://pypi.org/simple

# Core ML / numerics
numpy
packaging
PyYAML
h5py
matplotlib
ipython
pandas
imageio
mediapy
tqdm



# Robotics / simulation
pyquaternion
rospkg
pexpect
MuJoCo
dm-control

# Vision / geometry
opencv-python
open3d
plyfile
trimesh
scikit-image
pyrender
smplx
yacs
gdown
plotly
omegaconf
iopath





# Notes for packages that are usually installed separately because they are version-sensitive:
# 1) PyTorch3D is required by MCC-HO demos, but current official PyTorch3D install docs target newer
#    PyTorch versions than 2.0.0. Install a matching PyTorch3D build separately if pip install fails.
# pytorch3d
#
# 2) HaMeR's demo stack also expects the ViTPose submodule / MMPose stack.
#    The HaMeR repo recommends installing it separately:
#    pip install -v -e third-party/ViTPose
#
# 3) mujoco-mjx is a separate package that starts with MuJoCo 3.x and will conflict with mujoco==2.3.3.
#    Put it in a separate environment or upgrade mujoco if you need MJX.
# mujoco-mjx
# jax
# jaxlib